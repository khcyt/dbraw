import{initContext,kh_global}from"/contrib/jsm/kh_module_context.js";const cf="kh_web";const mf="kh_web_common_esm";const{cs,ms}=initContext(cf,mf);import{isString,isEmpty,isNode,isValid}from"/contrib/jsm/kh_earlybird.js";import{Error as khjs_Error}from"/contrib/jsm/kh_classes.js";const name=MF`${mf}`;const print_level=undefined;import{Logger}from"/contrib/jsm/kh_log.js";const kh_log=new Logger(print_level,()=>name);import{HTTPError}from"/contrib/jsm/kh_io.js";import{vjs}from"/contrib/jsm/kh_vanilla.js";export function unzip(storage,label_key,element=true){return!isString(label_key)?undefined:unzipProp(storage,label_key,true,element)}export function unzipProp(storage,prop,must_key=false,element=true){return globalThis.kh.storage.kh_js.unzipProp(prop,must_key,isNode()||element||getMainWindow(storage))}export function getMainWindow(storage){if(isNode()||!isValid(storage))return undefined;const key=`${storage.cf}-mainw`;if(!isValid(storage[key])){storage[key]=vjs(`#${unzip(storage,storage["key.id.main.win"]??"id.main.win",true)}`,false)}return storage[key]}export function processTagged(storage,strings,...keys){if(isEmpty(strings)&&isEmpty(keys))return"";return keys.map((key,i)=>{let s=unzip(storage,key);return strings[i]+(!isEmpty(s)?s:key)}).join("")+strings[strings.length-1]}export function processTaggedComplex(storage,strings,...keys){if(isEmpty(strings)&&isEmpty(keys))return"";return unzipProp(storage,processTagged(storage,strings,...keys))}export class Error extends(HTTPError(khjs_Error)){constructor(...argz){super(...arguments)}}export function nonEmptyAttr(attr){if(isEmpty(attr))return"";return`[${attr}]:not([${attr}=""])`}export function emptyAttr(attr,combine=false){if(isEmpty(attr))return"";return`${combine?":is(":""}:not([${attr}]),[${attr}=""]${combine?")":""}`}kh_global.LoadedScripts.get(mf).resolve(ms);